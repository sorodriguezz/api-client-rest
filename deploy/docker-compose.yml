services:
  mongo:
    image: mongo:7
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: postmanish
    volumes:
      - mongodata:/data/db

volumes:
  mongodata: